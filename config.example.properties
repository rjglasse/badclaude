# Copy this file to config.properties and fill in your values.
# config.properties is gitignored so your key never ends up in git.
#
# Any OpenAI-compatible endpoint works. Examples:
#   OpenAI:     base_url=https://api.openai.com/v1      model=gpt-4o-mini
#   OpenRouter: base_url=https://openrouter.ai/api/v1   model=<pick a cheap one>
#   Course proxy: your teacher will give you the values.

base_url=https://api.openai.com/v1
model=gpt-4o-mini

# Prefer setting the OPENAI_API_KEY environment variable instead of this:
# api_key=sk-...
